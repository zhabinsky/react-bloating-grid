{
  "name": "scripts",
  "version": "1.0.0",
  "description": "## [Documentation](https://zhabinsky.github.io/react-bloating-grid/)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch package/src/ --ignore ../package/node_modules/ --exec \"cd package && npm run build && cd ../docs-src && npm run sync && npm start\"",
    "compile-docs": "cd docs-src && npm run build && git add . && git commit -m 'updated the docs'"
  },
  "repository": {
    "type": "git",
    "url": " "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^4.0.10"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm run compile-docs"
    }
  }
}